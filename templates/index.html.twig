{% extends "base.html.twig" %}

{% block body %}
   <h1>{{ title }}</h1>
   <h2><a href="{{path("program_index")}}">Les séries</a></h2>
   <h2><a href="{{path("category_index")}}">Les catégories</a></h2>
{% endblock body %}
