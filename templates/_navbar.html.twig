<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <div class="img-brand">
      <img src="{{ asset('build/images/logo_WCS.jpeg') }}">
      <a class="navbar-brand" href="/">
        <h1>Wild Series</h1>
      </a>
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <div class="main-tab">
          <li class="nav-item">
            <a class="nav-link" href="{{ path('program_index') }}">Séries</a>
          </li>
          {% if is_granted("ROLE_ADMIN") %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('season_index') }}">Saisons</a>
          </li>
          {% endif %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('episode_index') }}">Épisodes</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button"
              data-bs-toggle="dropdown" aria-expanded="false">
              Catégories
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              {# {% for category in categories %}
                <li><a class="dropdown-item" href="{{ path("category_show", { categoryName: category.name }) }}">{{ category.name }}</a></li>
              {% else %}
                  Aucune série trouvée.
              {% endfor %} #}
              <li><a class="dropdown-item" href="#">Action</a></li>
              <li><a class="dropdown-item" href="#">Romantique</a></li>
              <li><a class="dropdown-item" href="#">Humour</a></li>
            </ul>
          </li>
        </div>
        <div class="log-in">
          {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('profile') }}">Mon profil</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('logout') }}">Déconnexion</a>
          </li>
          {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('login') }}">Connexion</a>
          </li>
          {% endif %}
        </div>
      </ul>
    </div>
  </div>
</nav>